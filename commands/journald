journalctl -ef
this will keep journal open on the screen

journalctl _SYSTEMD_UNIT=sshd.service or journalctl -u sshd
Display all messages generated by sshd

journalctl -b -1
only show messages from the last system boot. This is useful for searching for info about a system crash. This requires a persistant journal to be configured.

journalctl --since "2015-02-02 23:00:00" --until "2015-03-02 12:00:00"
Displays all messages between the two dates

